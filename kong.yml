_format_version: '3.0'
_transform: true

services:
    - name: s1
      url: http://host.docker.internal:3000
      routes:
          - name: r1
            paths:
                - /
            strip_path: true
